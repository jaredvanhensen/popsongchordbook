---
description: common scripts for regular development workflow
---

# Deploy the application to the server (production env)

// turbo
1. Update version field inside `index.html` by incrementing it.
2. update `changelog.html` with the latest changes and version.
3. Commit the code to git.
4. Push to origin.
